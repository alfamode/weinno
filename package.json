{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "codegen": "echo 🚀 Running In Select Mode ... && echo . && npx playwright codegen --channel=chrome https://newtest.weinno.ir",
    "test": "echo 🚀 Running Test Cases ... && echo . && npx playwright test --headed --trace on --project=simple",
    "test-debug": "echo 🚀 Running Test Cases ... && echo . && npx playwright test --debug --project=simple",
    "test-utils": "npx tsx tests/utils/utils.test.ts"
  },
  "keywords": [],
  "author": "AlieFazel",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.60.0",
    "tsx": "^4.22.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "dotenv": "^17.4.2"
  }
}
